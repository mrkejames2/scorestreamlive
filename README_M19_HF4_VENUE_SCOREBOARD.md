# M19 HF4 — Venue Scoreboard Display
Adds a read-only full-screen Venue Scoreboard at `/scoreboard/games/{game_id}` using existing authoritative public game state and Socket.IO. No database migration.

