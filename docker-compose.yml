version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: scorestreamlive-bootstrap
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1